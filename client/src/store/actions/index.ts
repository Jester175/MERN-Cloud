import { registration, auth, login } from "./user";
import { getFiles, createDir, uploadFile } from "./file";

export default {
  registration,
  login,
  auth,
  getFiles,
  createDir,
  uploadFile,
};
